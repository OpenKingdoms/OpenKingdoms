{
  "scrub_note": "Legacy statements below describe behaviour derived from analysis of the retail binary. ':NNNNN' citations are evidence pointers into a private reference that is not distributed — they mark where a claim can be re-checked by whoever holds it, and nothing here reproduces that file's expression. The binary ships stripped, so any legacy 'function name' quoted is an analyst- or tool-assigned label, not a real symbol; several such labels in this binary are actively misleading. Code shown in patch plans is TAK-RE implementation (proposed); the 'workflowProgress' block is our own tool output.",
  "summary": "Diagnose 5 playtest issues, return precise patch plans",
  "agentCount": 5,
  "logs": [],
  "result": [
    {
      "issue": "builder-resume",
      "diagnosis": "SYMPTOM (no HelpBuild resume): ingame.c click dispatch has no resume path. With no targeting mode, a left-click on ANY friendly unit — including an under-construction nanoframe — unconditionally selects it (src/ui/ingame.c:710-716, Units_SelectSingle/Toggle). The context cursor (src/ui/ingame.c:398-408) only distinguishes enemy (HUD_CMD_ATTACK) vs any hover (HUD_CUR_SELECT); never a repair cursor. The HUD Heal path (ingame.c:648-649 → Units_CommandRepairSelected, src/render/units.c:888-905) issues UNIT_CMD_REPAIR + target, but the REPAIR tick branch (units.c:4089-4105) heals at heal_time rate, charges NO mana, never clears under_construction, and never runs the completion block (units.c:4182-4207 — that only runs in the build_target path). So Heal on a nanoframe free-fills HP while the frame stays under_construction and keeps decaying — there is no working resume path at all. Legacy routes builder-clicks-on-friendly-nanoframe to HelpBuild (dig: docs/notes/2026-09-04-combat-mechanics.md, order issue :39143-39258).\n\nDISTANCE GATING (verified — it IS gated): feeding only happens in case UNIT_ANIM_BUILDING (units.c:4038, feed at 4135-4181), and that state is only chosen when arrived_at_site: d2 from builder to site centre <= (stand_back_px+32)^2, stand_back = footprint half-diagonal*8 + 12 (units.c:3943-4001); otherwise desired=MOVING toward a stand-back goal. enter_state re-stamps anim_state from the freshly computed desired every tick (units.c:3076-3105), so a builder ordered away (cmd=MOVE takes the 3931 branch) stops feeding immediately, nano_idle_ticks accrues, and decay DOES fire after the 600-tick grace (tick_nanoframe_decay, units.c:4331-4356; covered by test_ui_screens.c:1976 nanoframe_decay_refunds_mana). builddistance is NOT parsed into UnitDef (no hits in units.c/tak_unit.h; docs/DATA_FORMATS.md:543 documents it) — the footprint-based proxy is the current stand-in and works. So plan item (1) already exists; no feed bug.\n\nRESIDUAL DETACH BUG: Units_CommandMoveSelected (units.c:742-771, sets target=-1 at 762 but not build_target), Units_CommandMoveUnit (773-786), Units_CommandPatrolSelected (788-807), and Units_CommandStopSelected (961-979) all leave build_target set. The stale handle makes ai.c treat the builder as permanently busy (ai.c:266/395/429: `cmd_kind==UNIT_CMD_BUILD || build_target>=0` → refuse work), misroutes Units_FactoryEnqueue's idle check (units.c:1181), and can dangle onto a reused handle after the frame dies.",
      "patch_plan": "1) src/render/units.c — detach on new orders: in Units_CommandMoveSelected, Units_CommandMoveUnit, Units_CommandPatrolSelected, and Units_CommandStopSelected, add `u->build_target = -1;` next to the existing `u->target = -1;`. (Frame then idles and decays per tick_nanoframe_decay — already correct.)\n\n2) src/render/units.c — resume = HelpBuild via the repair command: in Units_CommandRepairSelected (line 888), after the unit_def_can_repair(d) check, branch on `g_units[target_handle].under_construction`: if set, issue a BUILD resume instead of REPAIR — `u->cmd_kind = UNIT_CMD_BUILD; u->build_target = (int16_t)target_handle; u->cmd_x/cmd_y = target world pos; u->target = -1; unit_clear_path(u);`. For immobile builders (d->max_velocity <= 0) skip unless already in range. The existing UNIT_CMD_BUILD state machine (walk to stand-back radius, mana-charged feed, completion + economy caps at units.c:4149-4207) then does everything. No change needed to the REPAIR tick branch.\n\n3) src/render/units.c + include/tak_unit.h — two thin accessors next to g_units_get_player (units.c:611), matching the existing opaque-to-UI convention (units.c:617 comment): `int Units_IsUnderConstruction(int handle)` (returns g_units[h].under_construction for alive==1, else 0) and `int Units_SelectionHasBuilder(void)` (loop g_selection; alive, player_id==1, def cap UNIT_CAP_BUILDER && worker_time>0 → 1). Declare both in include/tak_unit.h near Units_CommandRepairSelected (line 747).\n\n4) src/ui/ingame.c — click dispatch (line 710): change the friendly-click branch condition so a resume click wins: before `else if (hit >= 0 && g_units_get_player(hit) == 1)` insert `else if (hit >= 0 && g_units_get_player(hit) == 1 && Units_IsUnderConstruction(hit) && Units_SelectionHasBuilder() && !shift_held) { Units_CommandRepairSelected(hit); ig_play_order_ack(world, \"repair\"); }` (falls back to select when no builder is selected or shift is held).\n\n5) src/ui/ingame.c — context cursor (lines 398-408): in the hover branch, before the enemy/select choice add: `if (g_units_get_player(hover) == 1 && Units_IsUnderConstruction(hover) && Units_SelectionHasBuilder()) cur_id = HUD_CMD_HEAL;` — HUD_DrawCursorById already accepts targeting ids (include/tak_hud.h:54,109).\n\nOptional parity follow-up (not required for the fix): parse FBI `builddistance` into UnitDef (loader near units.c:1618) and use it in place of the +32 slack in the arrived_at_site radius (units.c:3974).",
      "files": [
        "C:\\Projects\\TAK-RE\\src\\render\\units.c",
        "C:\\Projects\\TAK-RE\\src\\ui\\ingame.c",
        "C:\\Projects\\TAK-RE\\include\\tak_unit.h"
      ]
    },
    {
      "issue": "factory-flow",
      "diagnosis": "Spawn: Units_BeginBuildingForUnit's factory_production branch (src/render/units.c:1139-1152) spawns the mobile product as a normal alive unit at the factory CENTRE (world_x=u->world_x, :1141-1143; :1137 TODO notes COB QueryBuildInfo build-spot is not used). Same spawn used by the idle-factory path in Units_FactoryEnqueue (:1181-1185), queue advance (:1224-1231, :4250-4259), and AI (src/game/ai.c:334). Render: build_draw_order deliberately INCLUDES under-construction units (units.c:4556-4566; comment :4559-4562 says skipping them \"left only the sparklies visible at empty air\" — but that IS legacy for the first half), and submit_run's only treatment is an alpha fade floored at 0.15 (units.c:4751-4763), so the nanoframe is visible from tick one. Legacy hard-gates: buildtimeleft >= 0.5 -> NOT drawn at all; < 0.5 -> drawn with a translucent blend mode (mode 6) and an alpha ramp 0->255; 0.0 -> opaque (docs/notes/2026-09-04-rendering-visuals.md, legacy :197310-197322; PARITY_MATRIX.md:65 flags this). render_health_bars (units.c:5289-5294) also draws a bar over the should-be-invisible frame. HUD: build slots are portrait-only, no queue badge (src/ui/hud.c:868-899, HUDBuildSlot :60-62; PARITY_MATRIX.md:134 = missing). Worse, HUD_HandleSidebarClick always enters PLACE_BUILD (hud.c:956-963) and the world click calls Units_BeginBuilding at the CLICK site (src/ui/ingame.c:676-696) — human factory production never routes through Units_FactoryEnqueue/the in-yard branch at all. Completion exit-walk + rally already exist and are render-independent: units.c:4208-4245 (rally wins when set, else first walkable of 4 exits at footprint*8+24px) and queue advance :4250-4259, gated on builder immobile + product mobile (:4216-4217). 60Hz note: hp/tick divides by buildtime*60 (units.c:4160,4174) and health grows linearly with spend (:1199-1203 comment), so health/max_health is a timebase-independent stand-in for legacy buildtimeleft — the 0.5 gate needs no rate conversion.",
      "patch_plan": "1) src/render/units.c build_draw_order (:4556-4566): add global nanoframe gate — skip unit when u->under_construction && u->max_health > 0 && u->health * 2 < u->max_health (legacy hides ALL nanoframes below 50%, legacy :197310); rewrite the :4559-4562 comment (sparklies-only during first half is legacy-correct). 2) units.c submit_run (:4756-4763): replace `a = 0.15f + 0.85f*t` with legacy second-half ramp: t = health/max; a = (t - 0.5f) * 2.0f clamped [0,1]; alpha_mul = a*255 (sub-50% units never reach here after edit 1; under_construction==0 stays 255/opaque). 3) units.c render_health_bars (after :5291 alive check): add the same sub-50% under-construction skip so no floating bar over a hidden frame (optionally same in render_selection_rings :5355). 4) units.c + include/tak_unit.h: new accessor `int Units_FactoryQueueCountForDef(int factory_handle, int def_idx)` next to Units_FactoryQueueCount (:1235-1238): bounds-check handle; count = occurrences of def_idx in f->prod_queue[0..prod_queue_len) + 1 if f->cmd_kind==UNIT_CMD_BUILD && f->build_target in [0,g_unit_count) && g_units[f->build_target].alive==1 && g_units[f->build_target].def_idx==def_idx; declare in tak_unit.h near :691. 5) src/ui/hud.c HUD_HandleSidebarClick (:956-963): on build-slot hit, fetch first selected handle/def (Units_GetSelection + g_units_get_def_idx as at :828-832); if selected def immobile (max_velocity <= 0) and clicked def mobile (max_velocity > 0) -> Units_FactoryEnqueue(handle, bs->def_idx), GameSound_PlayUI(\"addbuild\"), return 1 WITHOUT HUD_BeginBuildPlacement (fixes human production routing to the in-yard path; makes badges live). 6) src/ui/hud.c build-slot draw loop (:868-899): after `win` is computed (:887), if selected def is immobile, cnt = Units_FactoryQueueCountForDef(handle, g_build_list[b]); if cnt > 0, snprintf \"%d\" and overlay_text(plat, win.x + win.w - HUDText_Measure(g_text,buf) - 2, win.y + 2, buf, (SDL_Color){230,215,170,255}) — window coords like the UnitText overlay :769-777; optional 2px dark fill_rect_w backing. `handle` = sel2[0] already in scope (:830). 7) Verify (no code change expected): factory test test_ui_screens.c:1515-1570 (enqueue/rally/exit) still passes — completion path units.c:4216-4245 and queue advance :4250-4259 are untouched by the render gate; optionally add a probe asserting a fresh in-yard product (<50% hp) is absent from draw order and appears at >=50%. Update PARITY_MATRIX.md rows 65 (🔶 -> ✅) and 134 (badge part).",
      "files": [
        "C:\\Projects\\TAK-RE\\src\\render\\units.c",
        "C:\\Projects\\TAK-RE\\src\\ui\\hud.c",
        "C:\\Projects\\TAK-RE\\include\\tak_unit.h",
        "C:\\Projects\\TAK-RE\\docs\\PARITY_MATRIX.md"
      ]
    },
    {
      "issue": "attack-ground",
      "diagnosis": "Attack-ground doesn't exist anywhere in the pipeline; the command layer, weapon tick, and projectile system are all unit-handle-centric. (1) Dispatch: ingame.c:645-647 — HUD_CMD_ATTACK does `if (hit >= 0) Units_CommandAttackSelected(hit);` — a terrain click (Units_PickAt miss, ingame.c:630) is silently dropped. Even a friendly-building hit no-ops because Units_CommandAttackSelected rejects non-enemies (units.c:879-880) and command_attack_unit_ex likewise (units.c:834). (2) Command layer: the only attack kind is UNIT_CMD_ATTACK (tak_unit.h:332) which stores a target handle; the tick's desired-state chain (units.c:3862-3955) derives ATTACKING solely from `u->target >= 0` (units.c:3905-3930); there is no branch that ranges/aims against a cmd_x/cmd_y position. (3) Firing: the ATTACKING case (units.c:4265-4313) aims via weapon_aim_ready(u, slot, u->target, ws) (units.c:3552, 4280) which computes rel_heading from the target unit's coords (units.c:3567-3577), and fire_weapon_shot returns immediately if target_handle < 0 (units.c:3624). Burst also requires a live target unit (units.c:4301, 3694-3701). (4) Projectiles: spawn_projectile stores only a direction, not the destination (units.c:313-317, 341 — tx/ty are discarded after normalizing), and Projectile has no dest field (tak_unit.h:172-190). tick_projectiles hit-tests ONLY `p->target >= 0` (units.c:484-510); a target==-1 projectile just flies until TTL expiry (units.c:476) with no detonation. (5) Damage filters: apply_projectile_area_damage skips any victim not an enemy of the shooter (units.c:436), so friendly buildings can never take splash; features are never damaged — MapFeature (tak_world.h:122-128) has no HP and nothing ever removes entries (render at units.c:5715, walkability at terrain.c:296-301 both just iterate the static array). Per docs/notes/2026-09-04-combat-mechanics.md, legacy attack-ground is weapon fire at a position whose splash hits everything (friendly fire on explosive weapons).",
      "patch_plan": "1) include/tak_unit.h: add `#define UNIT_CMD_ATTACK_GROUND 10` after UNIT_CMD_UNLOAD (:339). In Projectile (:172): add `int32_t dest_x, dest_y;` and turn `_pad[1]` into `uint8_t friendly_fire;`. Declare `void Units_CommandAttackGroundSelected(int32_t world_x, int32_t world_y);` near Units_CommandAttackSelected (:755).\n\n2) include/tak_world.h struct MapFeature (:122): add `int32_t hp;`. src/ui/loading.c (:523-527): init `world->features[k].hp = (gidx >= 0 && Features_GetByIndex(gidx)->damage > 0) ? Features_GetByIndex(gidx)->damage : 1;`.\n\n3) src/render/units.c:\n- New Units_CommandAttackGroundSelected(x,y): loop selection like Units_CommandAttackSelected (:869-886) minus enemy/fog checks: for each selected alive player-1 unit with def->num_weapons > 0 set cmd_kind=UNIT_CMD_ATTACK_GROUND, target=-1, cmd_x=x, cmd_y=y, unit_clear_path. (Persists until stop/move: Units_CommandStopSelected :962-973 and Move/other commands already overwrite cmd_kind, giving \"continuous until new order\".)\n- spawn_projectile (:290-352): store `p->dest_x=tx; p->dest_y=ty; p->friendly_fire = (target_handle < 0);`.\n- apply_projectile_area_damage (:428-451): change the filter at :436 to `if (!p->friendly_fire && !unit_players_are_enemies(...)) continue;` (credit_kill already refuses same-player XP :397).\n- New static damage_features_at(int32_t x, int32_t y, int radius, const Projectile *p): world=World_Get(); for each world->features[i], center wx=tile_x*16+fp_x*8, wy=tile_z*16+fp_z*8 (same anchor math as render_features :5723-5732); if dist2 <= radius^2 and fd=Features_GetByIndex(global_idx) and fd && !fd->indestructible, subtract Units_ComputeSplashDamage(p->damage, radius, p->edge_effectiveness, d2) from features[i].hp; on <=0 swap-remove: `world->features[i--] = world->features[--world->feature_count];` (render and Terrain_IsWalkable iterate this array live, so blocking/draw update automatically).\n- tick_projectiles (:472-517): add `else if (p->target < 0)` branch after the :484 block: d2 = point_segment_dist2_i32(p->dest_x, p->dest_y, old_x, old_y, p->world_x, p->world_y); if d2 <= 24*24 → snap world_x/y to dest, play_projectile_hit_sound; if area_of_effect > 0 → apply_projectile_area_damage(p) + damage_features_at(dest_x, dest_y, aoe, p); else → damage the nearest alive unit within 24px of dest regardless of team (direct-hit path mirroring :497-501, using projectile_base_damage_for_unit) and damage_features_at(dest,16,p); set p->alive=0. (TTL from :344 is 1.5x travel time so arrival always precedes expiry.)\n- weapon_aim_ready (:3552-3618): change signature to (Unit*, int slot, int aim_key, int32_t aim_x, int32_t aim_y, UnitWeaponState*): compute rel_heading from (aim_x - u->world_x, aim_y - u->world_y) instead of dereferencing target (:3567-3577); use aim_key for the :3581 change-detect (`ws->aim_target != aim_key`). Existing call site :4280 passes (u->target, t->world_x, t->world_y); ground path passes (-2, u->cmd_x, u->cmd_y) — -2 fits int16 aim_target.\n- fire_weapon_shot (:3620-3683): allow ground shots — add explicit tx/ty params (or a sibling fire_weapon_ground_shot): when target_handle < 0, skip the :3624-3626 guard and the melee branch (:3632, melee no-ops at ground), aim spray around (tx,ty) directly (replace t->world_x/y at :3652-3653), and spawn_projectile(..., target_handle=-1, ...) toward (tx,ty).\n- Desired-state chain: insert before `else if (u->target >= 0)` (:3905): `else if (u->cmd_kind == UNIT_CMD_ATTACK_GROUND)` — dx/dy to cmd_x/cmd_y; same active-slot range/min_range check as :3913-3922; in range → target_in_range=1, desired=UNIT_ANIM_ATTACKING; else desired=MOVING, goal=cmd_x/y; set heading (:3930 pattern).\n- ATTACKING case (:4265-4313): compute aim triple — ground: (-2, cmd_x, cmd_y), unit: (u->target, t->world_x, t->world_y); pass to weapon_aim_ready; on fire, ground path calls the ground fire variant at (cmd_x, cmd_y); mana gating/cooldown (:4287-4311) unchanged. Burst: for ground set ws->burst_target = -2; in tick_weapon_burst (:3685-3710) treat burst_target == -2 as valid while u->cmd_kind == UNIT_CMD_ATTACK_GROUND (else cancel), firing the ground variant at u->cmd_x/cmd_y with burst_ordinal.\n- Optional polish: unit_status_string (:577) add case UNIT_CMD_ATTACK_GROUND → \"Engaging\".\n\n4) src/ui/ingame.c (:645-647): replace with — enemy hit routes to unit attack, everything else (terrain, friendly, neutral) to ground attack at the click point:\n`case HUD_CMD_ATTACK: if (hit >= 0 && g_units_get_player(hit) != 1) Units_CommandAttackSelected(hit); else Units_CommandAttackGroundSelected(world_click_x, world_click_y); break;`\nThe \"attack\" ack at :702 already covers both.",
      "files": [
        "C:\\Projects\\TAK-RE\\include\\tak_unit.h",
        "C:\\Projects\\TAK-RE\\include\\tak_world.h",
        "C:\\Projects\\TAK-RE\\src\\render\\units.c",
        "C:\\Projects\\TAK-RE\\src\\ui\\ingame.c",
        "C:\\Projects\\TAK-RE\\src\\ui\\loading.c"
      ]
    },
    {
      "issue": "mana-economy",
      "diagnosis": "SYMPTOM: lodestones yield flat 10 mana/s each; legacy yields mogriumincome x sacredsite-tier (10/15/20 for ARALODE, up to 40/s for ARAMANA). The missing sacred-site multiplier is the bug; the per-second plumbing itself is correct.\n\nWHAT THE CODE DOES TODAY:\n(1) FBI parse: src/render/units.c:1636-1639 reads maxmana (int), manarechargerate (float/s), mogriumstorage (int), mogriumincome (float/s) — unscaled per-second, matching legacy load (legacy :162891-162894 stores the mogrium* keys unscaled; the tick scales by 1/30 per 30Hz frame at :235965). aralode.fbi: mogriumincome=10, mogriumstorage=1000; araking.fbi: maxmana=1000, manarechargerate=20, mogriumincome=10, mogriumstorage=5000. `yardmap` is never parsed anywhere in src/.\n(2) Build completion (units.c:4182-4201): when bt->health >= max, Economy_AdjustCaps(+max_mana+mogrium_storage, +mana_recharge_per_sec+mogrium_income_per_sec) — ARALODE => +1000 cap, +10/s. Death retracts symmetrically (units.c:3139-3151). Economy_Tick (src/game/economy.c:147-153) adds regen_per_sec/60 per tick at a true 60Hz fixed step (ingame.c:369-371 while-loop; sim_dt=1/60), so 10/s parses through correctly. Caps AND regen are both applied; income is applied (cap-clamped) — none of the hinted per-minute/monarch-only/caps-without-regen failure modes exist.\n(3) LEGACY (behaviour recovered from :235820-235985): per 30Hz frame, per player, income = the SUM over alive, completed units of — a unit WITHOUT the sacred-income def flag contributes its mogriumincome flat (:235894-235896); a unit WITH that flag instead scans its own footprint cells for the feature underneath it and contributes mogriumincome x that feature def's `sacredsite` value, but ONLY when the feature's entire footprint is covered by the unit (:235826-235843). The per-player total is added to the mana pool scaled by 1/30 per frame (:235965). The sacred-income def flag is the one set by yardmap char 'S' (:163237); aralode.fbi/aramana.fbi both author `yardmap = S;`. Sacred pads AraMana01/02/03 (data/extracted/data/features/aramon/aramana.tdf, category=Mana, 2x2, blocking absent) carry sacredsite = 1.0 / 1.5 / 2.0; the surrounding AraHenge01-03 (blocking=1, 4x3) have NO sacredsite. Our FeatureDef doesn't even parse sacredsite (src/game/features.c:78-90, include/tak_features.h).\n\nNUMERIC EXAMPLE (Aramon skirmish): legacy N ARALODEs on tier-2.0 pads = 10 (monarch flat) + 20N mana/s; ours = 30 (monarch, incl. wrongly-pooled manarechargerate) + 10N. N=3: 70 vs 60; N=5: 110 vs 80; N=8: 170 vs 110 — each lodestone earns HALF the legacy tier-2.0 rate (2/3 of tier-1.5), and the gap widens all game. ARAMANA divine (mogriumincome=20): legacy 40/s on a 2.0 pad vs our flat 20/s.\n\nSECONDARY DEVIATIONS: (a) monarch MaxMana/ManaRechargeRate are folded into the player pool (loading.c:838-843, units.c:4189-4192) but legacy scales ManaRechargeRate x1/30 at load (:162913-162915) and feeds it into a PER-UNIT mana reserve kept separate from the player pool (:8708 — the cloak/spell resource; PARITY_MATRIX row 138 already tracks this as missing) — legacy pool cap = SUM mogriumstorage only (floored 1.0), income = SUM mogriumincome only. This masks part of the shortfall early (our +30/s start vs legacy +10/s) while lodestones still under-earn. (b) AI expansion (src/game/ai.c:471-498) matches fd->category==\"mana\", which ALSO matches the blocking AraHenge decor features, then `ai_find_clear_site` builds NEXT TO the site — worth 0 income under legacy rules and mis-anchored today.",
      "patch_plan": "1) include/tak_features.h: add `float sacred_site;` to FeatureDef. src/game/features.c (~line 89, next to reclaimable/indestructible reads): `f->sacred_site = TDF_ReadFloat(t, \"sacredsite\", 0.0f);`\n\n2) include/tak_unit.h: add `uint8_t sacred_income;` to UnitDef (yardmap contains 'S'). src/render/units.c FBI parse (after line 1639, inside UNITINFO): `out->sacred_income = (strchr(TDF_ReadString(tdf, \"yardmap\", \"\"), 'S') != NULL);` (legacy: capital 'S' only, :163237).\n\n3) src/render/units.c: new helper `static float sacred_income_mult(const UnitDef *d, int32_t wx, int32_t wy)`: if !d->sacred_income return 1.0f; else compute the unit's footprint tile rect (x0=(wx - footprint_x*8)/16, z0=(wy - footprint_z*8)/16, dims footprint_x/z); iterate World_Get()->features[]; for each with Features_GetByIndex(global_idx)->sacred_site > 0.0f, count its footprint cells (feature rect from tile_x/tile_z + footprint_x/z) inside the unit rect; if count >= fd->footprint_x*fd->footprint_z return fd->sacred_site (legacy full-coverage rule :235838-235843); after loop return 0.0f.\n\n4) src/render/units.c build completion (~4191): change regen_delta to `btd->mana_recharge_per_sec + btd->mogrium_income_per_sec * sacred_income_mult(btd, bt->world_x, bt->world_y);` Keep cap_delta as-is.\n\n5) src/render/units.c apply_killed (~3143): apply the same multiplier: `regen_delta = td->mana_recharge_per_sec + td->mogrium_income_per_sec * sacred_income_mult(td, t->world_x, t->world_y);` (buildings are static so add/remove amounts match; expose the helper via tak_units.h if loading.c needs it).\n\n6) src/ui/loading.c mission-placement spawn (~742-744) and monarch spawn (~838-841): multiply d->mogrium_income_per_sec by the same helper at the spawn position (campaign maps pre-place lodestones on pads; monarch/non-S units get 1.0 so behavior is unchanged for them).\n\n7) src/game/ai.c ai_try_expand_to_sacred_site (~471-499): filter targets by `fd->sacred_site > 0.0f` instead of category==\"mana\" (excludes blocking AraHenge decor); aim at the pad center; delete the ai_find_clear_site off-site fallback (an off-pad lodestone now earns 0) — if the pad is blocked, return 0 and let the AI pick another site.\n\n8) Tests: src/game/test_economy.c — no change needed (AdjustCaps semantics unchanged). src/ui/test_ui_screens.c skirmish build test (~505-545): after adding a fake sacred feature under the build site, assert completion adds mogrium_income*2.0 to Economy_GetRegenRate for a 2.0-tier pad, and that an off-pad lodestone adds 0 regen but still +1000 cap. Update docs/PARITY_MATRIX.md Economy row (add sacredsite multiplier row, cite legacy :235826-235905 / :163237).\n\n9) OPTIONAL follow-up (separate commit, changes game feel): stop pooling monarch maxmana/manarechargerate (loading.c:838-841, units.c:4189-4192, units.c:3142-3143) and implement per-unit mana per PARITY_MATRIX row 138; legacy pool = SUM mogriumstorage (floor 1.0) + SUM mogriumincome only. Doing this WITHOUT step 3-6 would make the reported symptom worse (start income 30->10/s), so land the multiplier first.",
      "files": [
        "C:\\Projects\\TAK-RE\\include\\tak_features.h",
        "C:\\Projects\\TAK-RE\\src\\game\\features.c",
        "C:\\Projects\\TAK-RE\\include\\tak_unit.h",
        "C:\\Projects\\TAK-RE\\src\\render\\units.c",
        "C:\\Projects\\TAK-RE\\src\\ui\\loading.c",
        "C:\\Projects\\TAK-RE\\src\\game\\ai.c",
        "C:\\Projects\\TAK-RE\\src\\ui\\test_ui_screens.c",
        "C:\\Projects\\TAK-RE\\docs\\PARITY_MATRIX.md"
      ]
    },
    {
      "issue": "browser-perf-music",
      "diagnosis": "(A) WASM permanent slowdown = accumulator SATURATION lock-in, not unbounded debt. The browser loop is rAF-driven (src/main.c:400 emscripten_set_main_loop_arg fps=0); rAF stops entirely while the tab is hidden, so on refocus SDL_GetPerformanceCounter jumps by minutes. Timer_Update (src/core/game_loop.c:24-37) DOES clamp both frame_dt and the accumulator to max_ticks_per_frame*sim_dt = 4*16.67ms = 66.7ms (and build-wasm/ is from today, so the clamps are in the playtested binary). The bug: the clamp leaves the accumulator PEGGED at the 4-tick cap, so the resume frame runs 4 sim ticks (src/ui/ingame.c:369 while(Timer_ConsumeTick)). One sim tick is expensive in WASM: InGame_SimulationStep (src/ui/ingame.c:211-234) runs AI + engines + economy + Fog_Update for every open player, and Fog_Update (src/game/fog.c:104-144) is O(units x sight^2 cells) with a per-cell los_clear heightmap raycast — ~10-20ms/tick mid-game. So a 4-tick frame costs 4*c_tick+render; whenever that, rounded up to the next vsync slot rAF gives you, is >= 66.7ms, the next Timer_Update measures dt >= 66.7ms, clamps it to exactly max_sim_dt, and refills the accumulator back to the cap -> 4 ticks again, every frame, forever. The system is bistable (normal: 1 tick @60fps; locked: 4 ticks @8-15fps with sim at/below real speed); tab-away is the only event that pegs the accumulator, which is why it is triggered by refocus and never recovers. Legacy avoided exactly this: 5 ticks/frame cap PLUS hysteresis auto-SlowDown when the cap is hit >10 frames in a row (docs/notes/2026-09-04-save-sound-gameflow.md, sim-tick budget routine :242330-242421; open item docs/PARITY_MATRIX.md:203 'no refocus slowdown'). Audio is ruled out: music never initializes in the browser (see B), TAK_Sound_Update (src/sound/sound.c:186-198) only garbage-collects finished channels, and miniaudio's WebAudio callback is independent of rAF.\n\n(B) No browser music: main.c:356 calls TAK_Music_Init(TAK_GAME_DIR); under Emscripten TAK_GAME_DIR=\"/game\" (src/CMakeLists.txt:157). scan_music_directory (src/sound/music.c:137-162) probes with raw fopen(\"%s/Music/track%d.wav\") — filesystem, not VFS. The preload set (src/CMakeLists.txt:168-183) stages only the GOG *.hpi files into ${CMAKE_BINARY_DIR}/wasm-game mounted at /game, plus the loose extracted tree at /data/extracted; no Music/ dir exists in MEMFS, so track_count==0 and TAK_Music_Init returns -1 (\"music system unavailable\"). Confirmed by docs/notes/2026-09-04-implementation-audit.md,50. VFS routing would be wrong: the 20 tracks are loose WAVs on disk (verified C:/GOG Games/Total Annihilation Kingdoms/Music = track1.wav..track20.wav, lowercase, unpadded — matching music.c:144's first probe), they are NOT inside any HPI, so the VFS has nothing to serve. Cheapest correct fix is pure CMake: stage Music/*.wav into the already-preloaded wasm-game staging dir so the existing --preload-file ...@/game ships them at /game/Music/; fopen then works against MEMFS with zero source changes (miniaudio MA_SOUND_FLAG_STREAM streams from MEMFS via stdio fine). Cost: ~60-100MB more .data download (packager data lives outside the 1GB fixed heap).",
      "patch_plan": "(A) src/core/game_loop.c, Timer_Update — add a pause guard that DROPS debt instead of saturating the accumulator, and keep the existing clamps. Replace the body with:\n    Uint64 counter = SDL_GetPerformanceCounter();\n    double dt = (double)(counter - t->last_counter) / (double)t->frequency;\n    t->last_counter = counter;\n    if (dt > 0.25) {                    /* rAF pause (hidden tab)/debugger: not gameplay time */\n        t->frame_dt = t->sim_dt;        /* sane dt for UI/anim consumers */\n        t->accumulator = 0.0;           /* drop the debt — do NOT fast-forward */\n        return;\n    }\n    if (dt > 0.1) dt = 0.1;             /* hitch cap: 100 ms */\n    t->frame_dt = dt;\n    t->accumulator += dt;\n    double max_acc = t->max_ticks_per_frame * t->sim_dt;\n    if (t->accumulator > max_acc) t->accumulator = max_acc;\nThis prevents refocus from ever pegging the accumulator, so the 4-tick lock-in regime is never entered. Optional hardening (legacy parity, digs:ssf :242330): if Timer_ConsumeTick returns max_ticks_per_frame ticks for >10 consecutive frames, reset accumulator to one sim_dt (hysteresis escape valve for non-tab-away load spikes).\nsrc/core/test_gameloop.c — the SDL_Delay(1000) test at line 80 currently asserts 4 ticks; with the pause guard a 1s gap now yields 0 ticks + accumulator==0 — update that assertion and add a test that a >250ms gap resets accumulator to 0 and frame_dt to sim_dt (100ms-gap test at line 48 still expects normal accumulation, unchanged).\n\n(B) src/CMakeLists.txt, EMSCRIPTEN block — after the existing file(COPY ${TAK_HPI_FILES} DESTINATION \"${TAK_WASM_HPI_DIR}\") at line 171, add:\n    file(GLOB TAK_MUSIC_FILES \"${TAK_GAME_DIR}/Music/*.wav\")\n    if(TAK_MUSIC_FILES)\n        file(COPY ${TAK_MUSIC_FILES} DESTINATION \"${TAK_WASM_HPI_DIR}/Music\")\n        message(STATUS \"WASM: bundling Music/ tracks\")\n    else()\n        message(WARNING \"WASM: no Music/*.wav found — browser music disabled\")\n    endif()\nThe existing \"SHELL:--preload-file ${TAK_WASM_HPI_DIR}@/game\" (lines 172-173) then exposes /game/Music/track1.wav..track20.wav in MEMFS; scan_music_directory's lowercase probe (music.c:144) matches the GOG filenames verbatim (MEMFS is case-sensitive, but names are already lowercase/unpadded). No C changes needed. Optional tidy: fix the stale comment in scripts/wasm_pre.js (claims no HPIs shipped; keep the harmless FS.mkdir).",
      "files": [
        "C:\\Projects\\TAK-RE\\src\\core\\game_loop.c",
        "C:\\Projects\\TAK-RE\\src\\core\\test_gameloop.c",
        "C:\\Projects\\TAK-RE\\src\\CMakeLists.txt",
        "C:\\Projects\\TAK-RE\\scripts\\wasm_pre.js"
      ]
    }
  ],
  "workflowProgress": [
    {
      "type": "workflow_phase",
      "index": 1,
      "title": "Diagnose"
    },
    {
      "type": "workflow_agent",
      "index": 1,
      "label": "builder-resume",
      "phaseIndex": 1,
      "phaseTitle": "Diagnose",
      "agentId": "ae0a7e7fcf09dd569",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1788550143729,
      "queuedAt": 1788550143712,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "SYMPTOM (no HelpBuild resume): ingame.c click dispatch has …",
      "promptPreview": "You are diagnosing a playtest issue in TAK-RE (C:\\Projects\\TAK-RE), a C rewrite of TA: Kingdoms. Read-only: do NOT edit files. Reference docs: docs/notes/2026-09-04-*.md (line-cited legacy reference digs), docs/PARITY_MATRIX.md. Sim runs at 60Hz (legacy 30Hz). Return SHORT structured output: diagnosis (what the code does today and why the symptom happens, with file:line cites), patch_plan (exact edits: fi…",
      "lastProgressAt": 1788550388016,
      "tokens": 80066,
      "toolCalls": 31,
      "durationMs": 244286,
      "resultPreview": "{\"diagnosis\":\"SYMPTOM (no HelpBuild resume): ingame.c click dispatch has no resume path. With no targeting mode, a left-click on ANY friendly unit — including an under-construction nanoframe — unconditionally selects it (src/ui/ingame.c:710-716, Units_SelectSingle/Toggle). The context cursor (src/ui/ingame.c:398-408) only distinguishes enemy (HUD_CMD_ATTACK) vs any hover (HUD_CUR_SELECT); never a …"
    },
    {
      "type": "workflow_agent",
      "index": 2,
      "label": "factory-flow",
      "phaseIndex": 1,
      "phaseTitle": "Diagnose",
      "agentId": "a6686c1ea48b957fa",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1788550143731,
      "queuedAt": 1788550143713,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Spawn: Units_BeginBuildingForUnit's factory_production bran…",
      "promptPreview": "You are diagnosing a playtest issue in TAK-RE (C:\\Projects\\TAK-RE), a C rewrite of TA: Kingdoms. Read-only: do NOT edit files. Reference docs: docs/notes/2026-09-04-*.md (line-cited legacy reference digs), docs/PARITY_MATRIX.md. Sim runs at 60Hz (legacy 30Hz). Return SHORT structured output: diagnosis (what the code does today and why the symptom happens, with file:line cites), patch_plan (exact edits: fi…",
      "lastProgressAt": 1788550405045,
      "tokens": 75587,
      "toolCalls": 40,
      "durationMs": 261314,
      "resultPreview": "{\"diagnosis\":\"Spawn: Units_BeginBuildingForUnit's factory_production branch (src/render/units.c:1139-1152) spawns the mobile product as a normal alive unit at the factory CENTRE (world_x=u->world_x, :1141-1143; :1137 TODO notes COB QueryBuildInfo build-spot is not used). Same spawn used by the idle-factory path in Units_FactoryEnqueue (:1181-1185), queue advance (:1224-1231, :4250-4259), and AI (s…"
    },
    {
      "type": "workflow_agent",
      "index": 3,
      "label": "attack-ground",
      "phaseIndex": 1,
      "phaseTitle": "Diagnose",
      "agentId": "ab8ad88b7623caabe",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1788550143731,
      "queuedAt": 1788550143713,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "Attack-ground doesn't exist anywhere in the pipeline; the c…",
      "promptPreview": "You are diagnosing a playtest issue in TAK-RE (C:\\Projects\\TAK-RE), a C rewrite of TA: Kingdoms. Read-only: do NOT edit files. Reference docs: docs/notes/2026-09-04-*.md (line-cited legacy reference digs), docs/PARITY_MATRIX.md. Sim runs at 60Hz (legacy 30Hz). Return SHORT structured output: diagnosis (what the code does today and why the symptom happens, with file:line cites), patch_plan (exact edits: fi…",
      "lastProgressAt": 1788550393341,
      "tokens": 89782,
      "toolCalls": 38,
      "durationMs": 249610,
      "resultPreview": "{\"diagnosis\":\"Attack-ground doesn't exist anywhere in the pipeline; the command layer, weapon tick, and projectile system are all unit-handle-centric. (1) Dispatch: ingame.c:645-647 — HUD_CMD_ATTACK does `if (hit >= 0) Units_CommandAttackSelected(hit);` — a terrain click (Units_PickAt miss, ingame.c:630) is silently dropped. Even a friendly-building hit no-ops because Units_CommandAttackSelected r…"
    },
    {
      "type": "workflow_agent",
      "index": 4,
      "label": "mana-economy",
      "phaseIndex": 1,
      "phaseTitle": "Diagnose",
      "agentId": "a05f1efe8334ca787",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1788550143732,
      "queuedAt": 1788550143713,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "SYMPTOM: lodestones yield flat 10 mana/s each; legacy yield…",
      "promptPreview": "You are diagnosing a playtest issue in TAK-RE (C:\\Projects\\TAK-RE), a C rewrite of TA: Kingdoms. Read-only: do NOT edit files. Reference docs: docs/notes/2026-09-04-*.md (line-cited legacy reference digs), docs/PARITY_MATRIX.md. Sim runs at 60Hz (legacy 30Hz). Return SHORT structured output: diagnosis (what the code does today and why the symptom happens, with file:line cites), patch_plan (exact edits: fi…",
      "lastProgressAt": 1788550839521,
      "tokens": 129549,
      "toolCalls": 55,
      "durationMs": 695789,
      "resultPreview": "{\"diagnosis\":\"SYMPTOM: lodestones yield flat 10 mana/s each; legacy yields mogriumincome x sacredsite-tier (10/15/20 for ARALODE, up to 40/s for ARAMANA). The missing sacred-site multiplier is the bug; the per-second plumbing itself is correct.\\n\\nWHAT THE CODE DOES TODAY:\\n(1) FBI parse: src/render/units.c:1636-1639 reads maxmana (int), manarechargerate (float/s), mogriumstorage (int), mogriuminc…"
    },
    {
      "type": "workflow_agent",
      "index": 5,
      "label": "browser-perf-music",
      "phaseIndex": 1,
      "phaseTitle": "Diagnose",
      "agentId": "ab4ccdefb815a3d67",
      "model": "claude-fable-5",
      "state": "done",
      "startedAt": 1788550143732,
      "queuedAt": 1788550143713,
      "attempt": 1,
      "lastToolName": "StructuredOutput",
      "lastToolSummary": "(A) WASM permanent slowdown = accumulator SATURATION lock-i…",
      "promptPreview": "You are diagnosing a playtest issue in TAK-RE (C:\\Projects\\TAK-RE), a C rewrite of TA: Kingdoms. Read-only: do NOT edit files. Reference docs: docs/notes/2026-09-04-*.md (line-cited legacy reference digs), docs/PARITY_MATRIX.md. Sim runs at 60Hz (legacy 30Hz). Return SHORT structured output: diagnosis (what the code does today and why the symptom happens, with file:line cites), patch_plan (exact edits: fi…",
      "lastProgressAt": 1788550745852,
      "tokens": 111002,
      "toolCalls": 33,
      "durationMs": 602120,
      "resultPreview": "{\"diagnosis\":\"(A) WASM permanent slowdown = accumulator SATURATION lock-in, not unbounded debt. The browser loop is rAF-driven (src/main.c:400 emscripten_set_main_loop_arg fps=0); rAF stops entirely while the tab is hidden, so on refocus SDL_GetPerformanceCounter jumps by minutes. Timer_Update (src/core/game_loop.c:24-37) DOES clamp both frame_dt and the accumulator to max_ticks_per_frame*sim_dt =…"
    }
  ],
  "totalTokens": 485986,
  "totalToolCalls": 197
}
